const employee = require('./employee');
const employeeAuth = require('./employee.auth');
const employeeValidation = require('./employes.validation');
const absences = require('./absences');

module.exports = {
    absences,
    employee,
    employeeAuth,
    employeeValidation
}
